mat-progress-bar.page-load(*ngIf='loading' mode='indeterminate')
.container-fluid: .row
    .col-lg-3.col-md-4.col-sm-6(*ngFor='let room of rooms'): a([routerLink]='link(room._id)'): mat-card: mat-card-content
        .flex
            span.fill
            span: i.fas.fa-home.fa-3x
            span.fill
        h1 {{ room.title }}
